var numero1 = parseInt(prompt("Ingresa numero 1: "));
/*
while (isNaN(numero1)) {
    numero1 = parseInt(prompt("Ingresa numero 1 (solo numeros): "));
}
document.write(`El numero es ${numero1}`)

*/

do {
    
} while (condition);